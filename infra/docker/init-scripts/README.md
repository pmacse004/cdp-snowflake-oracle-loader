# Oracle init-scripts directory
# SQL files placed here are executed by Oracle after the database is fully started.
# Files are run in alphabetical order.
# Place schema creation scripts here ONLY IF you want them auto-run on first start.
# For this project, Flyway manages all DDL — do not duplicate DDL here.
